# WebSockets package 

from app.websockets.connection_manager import ConnectionManager

# Create a single instance of ConnectionManager to be shared across the application
manager = ConnectionManager() 